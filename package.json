{
  "name": "aws-resource-manager",
  "version": "1.0.0",
  "description": "AWS Resource listing and tagging utility",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "start": "npm run build && node dist/main.js",
    "start:dev": "ts-node src/main.ts",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1489.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}